<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:pageModels="clr-namespace:AgricultureApp.MauiClient.PageModels"
             xmlns:resources="clr-namespace:AgricultureApp.MauiClient.Resources.Strings"
             xmlns:local="clr-namespace:AgricultureApp.MauiClient.Converters"
             x:Class="AgricultureApp.MauiClient.Pages.VerifyTwoFactorPage"
             x:DataType="pageModels:VerifyTwoFactorPageModel"
             Title="VerifyTwoFactorPage">
    <VerticalStackLayout>

        <Label Text="{x:Static resources:AppResources.EnterTwoFactorCode}"
               FontSize="20"
               HorizontalOptions="Center"
               Margin="0,20,0,20" />
        <Entry Text="{Binding TwoFactorCode}"
               Keyboard="Numeric"
               HorizontalOptions="Center"
               WidthRequest="200" />
        <Button Text="{x:Static resources:AppResources.VerifyButton}"
                Command="{Binding VerifyCommand}"
                BackgroundColor="#4A90E2"
                TextColor="White"
                CornerRadius="8"
                HeightRequest="50"
                WidthRequest="200"
                HorizontalOptions="Center"
                Margin="0,20,0,0" />
    </VerticalStackLayout>
</ContentPage>